name: Deploy to VM

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build and restart containers
        run: |
          cd ~/ecommerce-devops
          docker compose down
          docker compose up -d --build
